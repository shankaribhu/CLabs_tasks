Steps:
1. Import the postman collection in your respective postman applications.
2. Open the terminal and set the runable go code path.
3. Type the command (source exportdev.sh) this is use to get the env values.
4. (go mod tidy) respective package is downloaded.
5. Finally, Run the go run commant (go run cmd/main.do), the go server is running on the 3000 PORT, then check the APIs in POSTMAN applications.
